/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./index.html",           // ← الـ HTML في الـ root
        "./src/JS/**/*.js"],       // ← الـ JS files],
  theme: {
    extend: {},
  },
  plugins: [],
}

